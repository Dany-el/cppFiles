#include <iostream>
#include <random>
#include "A_function.h"
#include "B_function.h"
#include "C_function.h"

int main()
{
    srand(time(0));
    // A
    // A_function A_temp;
    // int array[3][3];
    // A_temp.Init(array,3);
    // A_temp.Print(array,3);
    // cout << '\n';
    // A_temp.Reverse(array,3);

    // B
    // B_function B_temp;
    // B_temp.sum(1, 2);
    // B_temp.diff(3, 2);
    // B_temp.product(2, 2);

    // C
    // C_function C_temp;

    // int const size = 10;
    // int array[size];
    // for (size_t i = 0; i < size; i++)
    // {
    //     array[i] = i + 1;
    // }
    

    // C_temp.print_rev_array(array,size);
    // C_temp.sum_array(array,size);
    // C_temp.prod_array(array,size);
    // C_temp.avg_array(array,size);
    // C_temp.range_array(array,size);

    return 0;
}